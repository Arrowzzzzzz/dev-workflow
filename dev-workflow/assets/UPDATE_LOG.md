# 更新记录 UPDATE_LOG

> 本文件用于记录每一次代码更新，供后续人工或 AI 接续维护时快速了解历史改动。
> 规则：**新记录追加在最上方**（倒序，最新在前）；每条记录严格按下方模板字段填写，保持格式统一。

---

## 模板（复制以下区块新增一条记录）

<!--
### [YYYY-MM-DD HH:MM] <简短标题>

- **类型**：Bug 修复 / 新需求 / 重构 / 性能优化 / 依赖升级 / 其他
- **标识**：<版本号 / commit / issue / PR 编号，可选>
- **背景 / 根因**：<Bug 的 root cause，或新需求的目的与背景>
- **改动范围**：
  - `path/to/file_a.ext`：<改了什么、为什么>
  - `path/to/file_b.ext`：<改了什么、为什么>
- **实现思路**：<核心逻辑与关键步骤简述>
- **关键决策 / 复用**：<复用了哪些现有代码或库；为何这样选；权衡取舍>
- **依赖变更**：<新增/升级/移除的依赖；无则填「无」>
- **影响与风险**：<影响的上下游、兼容性、需注意的回归点>
- **测试情况**：<跑了哪些测试、结果、覆盖的场景；全部通过 ✅ / 存在遗留 ⚠️>
- **后续待办**：<遗留问题或后续可优化项；无则填「无」>
-->

---

## 更新历史

### [2025-01-01 01:01] 示例：修复登录接口 token 过期未刷新问题

- **类型**：Bug 修复
- **标识**：#123 / commit abc1234
- **背景 / 根因**：token 到期后未触发刷新逻辑，根因是拦截器只在 401 时刷新，而后端返回的是 403，导致刷新分支从未命中。
- **改动范围**：
  - `src/utils/request.js`：调整响应拦截器，将刷新触发条件从仅 401 扩展为 401/403，并复用已有的 `refreshToken()` 方法。
- **实现思路**：在现有拦截器基础上补充状态码判断，复用既有刷新与重试队列逻辑，未新增模块。
- **关键决策 / 复用**：复用项目已有的 `refreshToken()` 与请求重试队列，避免重复实现；未引入新库。
- **依赖变更**：无
- **影响与风险**：影响所有走统一拦截器的请求；需回归验证正常请求、401、403 三种路径。
- **测试情况**：补充并运行拦截器相关单元测试，覆盖 401/403/正常 三场景，全部通过 ✅。
- **后续待办**：无

<!-- 新记录请添加在「更新历史」标题下方、示例记录上方，保持最新在前 -->
